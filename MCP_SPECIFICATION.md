# Model Context Protocol (MCP) 仕様書 v2025-06-18

## 概要

Model Context Protocol (MCP) は、Large Language Models (LLMs) にコンテキストを提供するアプリケーション間の標準化されたオープンプロトコルです。「AI アプリケーション用の USB-C ポート」として、AI モデルと様々なデータソース、ツールを接続する標準的な方法を提供します。

## アーキテクチャ

### コンポーネント

1. **MCP Host** - データにアクセスしたいプログラム（Claude Desktop など）
2. **MCP Client** - ホストアプリケーション内のコネクター、サーバーとの接続を維持
3. **MCP Server** - 特定の機能を公開するサービス
4. **Local Data Sources** - ローカルデータソース
5. **Remote Services** - リモートサービス

### アーキテクチャ原則

- **クライアント・サーバー アーキテクチャ** でホスト、クライアント、サーバー間のシームレスな通信を実現
- **プロトコル層** がメッセージフレーミングとリクエスト/レスポンスのリンクを処理
- **トランスポート層** が複数の通信メカニズムをサポート

## 基本プロトコル

### JSON-RPC 2.0 メッセージング

MCP は JSON-RPC 2.0 仕様に厳密に準拠し、3つの主要メッセージタイプを使用：

#### 1. リクエスト
```json
{
  "jsonrpc": "2.0",
  "id": "string | number",
  "method": "string",
  "params": {
    // メソッド固有のパラメータ（オプション）
  }
}
```

**要件:**
- 非nullの文字列または整数のIDを含む必要がある
- IDはセッション内で一意である必要がある

#### 2. レスポンス
```json
{
  "jsonrpc": "2.0",
  "id": "string | number",
  "result": {
    // 成功時の結果（オプション）
  },
  "error": {
    "code": "number",
    "message": "string",
    "data": "unknown（オプション）"
  }
}
```

**要件:**
- リクエストのIDと一致する必要がある
- `result` または `error` のいずれか一方のみを含む
- エラーコードは整数である必要がある

#### 3. 通知
```json
{
  "jsonrpc": "2.0",
  "method": "string",
  "params": {
    // メソッド固有のパラメータ（オプション）
  }
}
```

**要件:**
- IDを含んではならない
- 受信者からのレスポンスは期待しない

### トランスポート層

#### 1. Standard Input/Output (stdio)
- **用途**: ローカル統合とコマンドラインツール
- **実装**: 
  - クライアントがMCPサーバーをサブプロセスとして起動
  - サーバーは標準入力/出力経由でJSON-RPCメッセージを読み書き
  - メッセージは改行文字で区切る必要がある
  - サーバーは標準エラーにログを出力可能
- **要件**: メッセージはUTF-8エンコーディングである必要がある

#### 2. Streamable HTTP
- **用途**: ネットワーク経由の通信
- **実装**:
  - HTTP POSTリクエストでクライアントからサーバーへの通信
  - オプションでServer-Sent Events (SSE) でサーバーからクライアントへの通信
  - 単一のHTTPエンドポイントが必要
- **セキュリティ推奨事項**:
  - Originヘッダーの検証
  - localhostのみへのバインド
  - 適切な認証の実装
- **高度な機能**:
  - `Mcp-Session-Id` ヘッダーによるセッション管理
  - 接続断時の再開可能性と再配信機能
  - 複数同時接続のサポート

## 接続ライフサイクル

### 1. 初期化フロー

#### initialize リクエスト
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "initialize",
  "params": {
    "protocolVersion": "2025-06-18",
    "capabilities": {
      "roots": {},
      "sampling": {}
    },
    "clientInfo": {
      "name": "lightbug-mcp-client",
      "version": "1.0.0"
    }
  }
}
```

#### initialize レスポンス
```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "protocolVersion": "2025-06-18",
    "capabilities": {
      "tools": {}
    },
    "serverInfo": {
      "name": "lightbug-mcp-server",
      "version": "1.0.0"
    }
  }
}
```

#### initialized 通知
```json
{
  "jsonrpc": "2.0",
  "method": "initialized",
  "params": {}
}
```

### 2. メッセージ交換

- リクエスト・レスポンス及び通知パターンをサポート
- どちらの当事者もメッセージを送信可能
- 進行状況レポートによる長時間操作のサポート

### 3. 終了

- どちらの当事者も開始可能
- クリーンシャットダウンまたはトランスポート切断をサポート

## サーバー機能

### 1. リソース (Resources) - 実装保留

**注意**: リソース機能は現在の実装では保留されています。リソース関連のリクエストはエラーで応答されます。

<!-- 保留された機能の詳細は実装時に有効化 -->

### 2. ツール (Tools)

サーバーが実行可能機能をクライアントに公開するためのメカニズム。AI モデルが自動的に呼び出すことができる「モデル制御」設計。

#### ツール定義構造
```json
{
  "name": "string",
  "description": "string (オプション)",
  "inputSchema": {
    "type": "object",
    "properties": { ... }
  },
  "annotations": {
    "title": "string (オプション)",
    "readOnlyHint": "boolean (オプション)",
    "destructiveHint": "boolean (オプション)",
    "idempotentHint": "boolean (オプション)",
    "openWorldHint": "boolean (オプション)"
  }
}
```

#### ツール呼び出しフロー
1. クライアントが利用可能ツールを要求 (`tools/list`)
2. サーバーがツール定義で応答
3. クライアントが特定ツールを選択して呼び出し (`tools/call`)
4. サーバーがツールを実行して結果を返す

#### ツールタイプ例
- システム操作（シェルコマンド実行など）
- API統合
- データ処理ツール

### 3. プロンプト (Prompts) - 実装保留

**注意**: プロンプト機能は現在の実装では保留されています。プロンプト関連のリクエストはエラーで応答されます。

<!-- 保留された機能の詳細は実装時に有効化 -->

## クライアント機能

### 1. サンプリング (Sampling)

MCPの強力な機能で、サーバーがクライアント経由でLLM補完を要求することを可能にし、ユーザー制御を維持する。

#### コアワークフロー
1. サーバーがクライアントにサンプリング要求を送信
2. クライアントが要求をレビューし、潜在的に修正
3. クライアントがLLMからサンプリング
4. クライアントが補完をレビュー
5. クライアントがサーバーに結果を返す

#### 主要リクエストパラメータ
- **Messages**: ユーザー/アシスタント役割の会話履歴
- **Model Preferences**: モデル選択と優先度設定のヒント
- **System Prompt**: LLM用のオプショナルコンテキスト
- **Context Inclusion**: 追加コンテキストの制御
- **Sampling Parameters**: 温度、最大トークン数、停止シーケンス

#### 設計原則
- **ヒューマン・イン・ザ・ループ設計** によりユーザーが制御を維持
- クライアントはプロンプトと補完を修正または拒否可能
- テキストと画像コンテンツの両方をサポート
- セキュリティとプライバシーを考慮した設計

#### 高度なワークフロー例
- リソースの読み取りと分析
- コンテキストベースの意思決定
- 構造化データの生成
- マルチステップタスクの処理
- インタラクティブなアシスタンス提供

### 2. ルーツ (Roots)

- **目的**: URI やファイルシステムの境界に関する問い合わせ
- **セキュリティ**: アクセス範囲の制限
- **実装**: サーバーがアクセス可能なリソースの範囲を定義

### 3. 誘導 (Elicitation)

- **目的**: 追加のユーザー情報の要求
- **インタラクション**: ユーザーとの対話的なやり取り
- **機能**: 動的な情報収集とユーザー入力の促進

## エラーハンドリング

### 標準JSONRPCエラーコード

- `-32700`: Parse error（パース エラー）
- `-32600`: Invalid Request（無効なリクエスト）
- `-32601`: Method not found（メソッドが見つからない）
- `-32602`: Invalid params（無効なパラメータ）
- `-32603`: Internal error（内部エラー）
- `-32000` to `-32099`: Server error（サーバーエラー）

### カスタムエラー例

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "error": {
    "code": -32001,
    "message": "Resource not found",
    "data": {
      "uri": "file:///path/to/missing/file.txt",
      "reason": "File does not exist"
    }
  }
}
```

### エラーハンドリング要件

- すべての受信メッセージの検証
- 適切なアクセス制御の実装
- リソースパスの保護
- リクエストの監視とレート制限

## セキュリティ原則

### 1. ユーザーの同意と制御

- **明示的なユーザー同意**: すべてのデータアクセスと操作に対する明確な許可
- **明確な承認フロー**: 透明な認証とアクセス制御プロセス
- **ユーザーによるアクセス制御**: ユーザーが権限を管理し、取り消し可能

### 2. データプライバシー

- **最小権限の原則**: 必要最小限のデータアクセスのみ許可
- **データアクセスの透明性**: アクセスされるデータの明確な表示
- **適切な暗号化**: 機密データの保護

### 3. ツールの安全性

- **ツール実行の制限**: 危険なコマンドの実行防止
- **入力検証**: すべてのツールパラメータの検証
- **コマンドインジェクション対策**: 悪意あるコード実行の防止
- **サンドボックス化**: 分離された実行環境

### 4. LLM サンプリング制御

- **再帰的実行の制限**: 無限再帰の防止
- **リソース使用量の監視**: CPU、メモリ、ネットワーク使用量の制御
- **無限ループの防止**: 適切なタイムアウトと制限の実装

### 5. ネットワークセキュリティ

- **Originヘッダー検証**: HTTPトランスポートでの不正アクセス防止
- **localhostバインド**: 外部アクセスの制限
- **適切な認証**: 強力な認証メカニズムの実装

## 実装時の考慮事項

### パフォーマンス

- **接続プールの管理**: 効率的な接続再利用
- **メッセージの並列処理**: 複数リクエストの同時処理
- **リソースキャッシュの最適化**: 頻繁にアクセスされるリソースのキャッシング
- **メモリ管理**: 効率的なメモリ使用と解放

### 拡張性

- **モジュラーなツール設計**: 独立したツールコンポーネント
- **プラグイン アーキテクチャ**: 動的な機能追加
- **動的機能登録**: 実行時の機能登録・削除
- **水平スケーリング**: 複数サーバーインスタンスの負荷分散

### 互換性

- **プロトコルバージョニング**: 複数バージョンのサポート
- **後方互換性の維持**: 既存クライアントとの互換性
- **機能のグレースフルデグラデーション**: 未サポート機能の適切な処理
- **型安全性**: 厳密な型チェックとバリデーション

## Lightbug HTTP での実装方針

### 1. アーキテクチャ統合

- **既存HTTPサーバー基盤の活用**: lightbug_httpの高性能HTTP実装を基盤として使用
- **ストリーマブルHTTPトランスポート**: HTTP POST及びServer-Sent Events (SSE) を実装
- **Mojoの型システム活用**: 型安全なMCP実装でランタイムエラーを最小化
- **モジュラー設計**: MCP機能を独立したモジュールとして実装

### 2. 機能要件

#### 必須機能
- **JSON-RPC 2.0メッセージング**: 完全なJSONRPC準拠
- **初期化フロー**: initialize/initializedハンドシェイク
- **ツールシステム**: 動的ツール登録・実行・管理
- **エラーハンドリング**: 標準及びカスタムエラーレスポンス

#### 保留機能
- **リソース管理**: 将来の実装予定
- **プロンプトシステム**: 将来の実装予定

#### セキュリティ機能
- **認証・認可**: 適切なアクセス制御
- **入力検証**: すべてのパラメータの厳密な検証
- **サンドボックス実行**: 安全なツール実行環境
- **リソース制限**: CPU・メモリ・ネットワーク使用量制御

### 3. パフォーマンス目標

- **低レイテンシー**: < 10ms の応答時間
- **高スループット**: > 10,000 RPS の処理能力
- **メモリ効率**: 最小限のメモリフットプリント
- **並行処理**: 数千の同時接続をサポート

### 4. 実装ロードマップ

#### Phase 1: 基盤実装
- JSON-RPC 2.0 メッセージング
- HTTP トランスポート層
- 基本的な初期化フロー

#### Phase 2: コア機能
- ツールシステム
- エラーハンドリング

#### Phase 3: 高度な機能
- Server-Sent Events
- セッション管理
- パフォーマンス最適化

#### Phase 4: セキュリティ強化
- 認証・認可システム
- サンドボックス実行
- セキュリティ監査

## 実装状況 (2025年更新)

### ✅ Phase 1: 基盤実装 - 完了

#### 実装完了項目
1. **JSON-RPC 2.0メッセージング基盤** (`/lightbug_http/mcp/jsonrpc.mojo`)
   - JSONRPCRequest, JSONRPCResponse, JSONRPCNotification構造体
   - 標準エラーコード実装 (-32700 to -32603)
   - 型安全なバリデーション機能
   - UTF-8エンコーディング対応

2. **メッセージパーサー・シリアライザー** (`/lightbug_http/mcp/parser.mojo`)
   - Python JSONモジュール連携
   - Variant型による統一メッセージ処理
   - 包括的エラーハンドリング
   - メッセージタイプ自動判定

3. **MCPメッセージ構造** (`/lightbug_http/mcp/messages.mojo`)
   - プロトコルバージョン管理 (2025-06-18)
   - Capabilities システム
   - 各MCPメソッド用ヘルパー関数
   - メッセージメタデータ管理

4. **HTTPトランスポート層** (`/lightbug_http/mcp/transport.mojo`)
   - Streamable HTTP実装 (POST専用)
   - Origin ヘッダー検証
   - CORS プリフライト対応
   - Content-Type検証 (application/json)
   - セキュリティヘッダー設定

5. **MCPサーバーコア** (`/lightbug_http/mcp/server.mojo`)
   - 接続ライフサイクル管理
   - 状態遷移システム (CONNECTING → INITIALIZING → READY)
   - リクエストルーティング
   - ハンドラー分離アーキテクチャ

6. **初期化ハンドシェイク**
   - initialize リクエスト処理
   - プロトコルバージョン検証
   - 機能ネゴシエーション
   - initialized 通知対応

#### 技術的成果
- **型安全性**: Mojoの強力な型システムを活用した実装
- **エラーハンドリング**: JSONRPCエラー仕様完全準拠
- **モジュラー設計**: 各コンポーネントの独立性確保
- **セキュリティ**: 適切な入力検証と不正アクセス防止
- **パフォーマンス**: 効率的な接続管理とメッセージ処理

#### 実装ファイル
- `/lightbug_http/mcp/__init__.mojo` - モジュールエクスポート
- `/lightbug_http/mcp/jsonrpc.mojo` - JSON-RPC 2.0基盤
- `/lightbug_http/mcp/parser.mojo` - メッセージ処理
- `/lightbug_http/mcp/messages.mojo` - MCP固有メッセージ
- `/lightbug_http/mcp/transport.mojo` - HTTPトランスポート
- `/lightbug_http/mcp/server.mojo` - サーバーコア
- `/examples/mcp_server_example.mojo` - 使用例

### 🔄 Phase 2: コア機能 - 実装予定

以下の機能が次の実装対象:
- 機能ネゴシエーション システム
- ツールシステム (定義・実行・管理)
- エラーハンドリング強化

**保留機能**:
- リソース管理 (URI解決・アクセス制御) - 将来実装
- プロンプトシステム - 将来実装

### 📊 実装進捗

- **Phase 1**: ✅ 100% 完了 (6/6項目)
- **Phase 2**: ⏳ 0% 完了 (0/10項目)
- **Phase 3**: ⏳ 0% 完了 (0/4項目)  
- **Phase 4**: ⏳ 0% 完了 (0/4項目)

**総合進捗**: 23% 完了 (6/26項目)